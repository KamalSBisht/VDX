import { Font, woff2 } from 'fonteditor-core';
import fs from 'fs';

console.log('woff2', woff2);

try {
  // Let's test with some dummy data or an existing font if possible
  console.log("Ready");
} catch(e) {
  console.log("Error", e);
}
